# Introduction

This chapter introduces key concepts. The API provides a simple interface for developers.

## REST Services

REST is a popular architectural style. Many API implementations use REST principles.

The API can return data in JSON format. JSON is widely supported across platforms.

## File Formats

The XPT format is commonly used in regulatory contexts. XPT files contain tabular data.

Use the CLI to interact with the system. The CLI supports multiple commands.
