# Nested Chapter

This chapter is in a nested directory. The relative path to the glossary should work correctly.

## Using the API

The API supports multiple formats including JSON and XPT.

REST endpoints are available for all operations. The CLI can be used for automation.
